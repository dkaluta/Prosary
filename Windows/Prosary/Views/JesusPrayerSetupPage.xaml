<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Prosary.Views.JesusPrayerSetupPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Padding="12,8" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Command="{x:Bind ViewModel.BackCommand}" Background="Transparent" Padding="8">
                <FontIcon Glyph="&#xE72B;" MirroredWhenRightToLeft="True" />
            </Button>
            <TextBlock x:Uid="JpTitle" Grid.Column="1" Text="The Jesus Prayer" VerticalAlignment="Center" HorizontalAlignment="Center" Style="{StaticResource SubHeadlineTextStyle}" />
        </Grid>

        <StackPanel Grid.Row="1" Spacing="16" MaxWidth="360" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="24">
            <TextBlock x:Uid="JpHowMany" Text="How many times?" HorizontalAlignment="Center" />

            <ComboBox HorizontalAlignment="Stretch" ItemsSource="{x:Bind ViewModel.Options}" SelectedItem="{x:Bind ViewModel.Selection, Mode=TwoWay}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Converter={StaticResource SetupOptionLabel}}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <TextBox x:Uid="JpRepetitions" Header="Number of repetitions" InputScope="Number"
                     Text="{x:Bind ViewModel.CustomCountText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     Visibility="{x:Bind ViewModel.IsCustomSelected, Mode=OneWay, Converter={StaticResource BoolToVisibility}}" />

            <Button x:Uid="JpBegin" Content="Begin" HorizontalAlignment="Stretch" Style="{StaticResource AccentButtonStyle}"
                    Command="{x:Bind ViewModel.BeginCommand, Mode=OneWay}" />
        </StackPanel>
    </Grid>
</Page>
