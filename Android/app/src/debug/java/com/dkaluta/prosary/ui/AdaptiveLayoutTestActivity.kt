package com.dkaluta.prosary.ui

import androidx.activity.ComponentActivity

/** Empty host for adaptive-layout tests; never initializes the person's prayer store. */
class AdaptiveLayoutTestActivity : ComponentActivity()
